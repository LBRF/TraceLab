[project]
name = "TraceLab"
version = "1.6"
requires-python = ">=3.10,<3.15"
dependencies = [
    "klibs @ https://github.com/a-hurst/klibs/releases/download/0.7.8b2/klibs-0.7.8b2.tar.gz",
]

[dependency-groups]
dev = [
    "pylint",
]

[tool.uv]
preview-features = ["centralized-project-envs"]
